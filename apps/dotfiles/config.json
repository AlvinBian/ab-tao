{
	"targets": {
		"claude-dev": {
			"label": "Claude Code 開發規則",
			"hint": "commands / agents / hooks / rules → ~/.claude/ + plugin",
			"flag": "--claude",
			"steps": [
				{
					"type": "install-claude",
					"label": "安裝 commands · agents · rules → ~/.claude/",
					"script": "node:config-sync",
					"selectable": {
						"commands": {
							"dir": "claude/commands",
							"ext": ".md",
							"selectLabel": "Slash Commands（/xxx）"
						},
						"agents": {
							"dir": "claude/agents",
							"ext": ".md",
							"selectLabel": "Agents（@xxx）"
						}
					},
					"fixed": {
						"rules": "all",
						"hooks": true
					},
					"hooksConfirm": true
				},
				{
					"type": "build-plugin",
					"label": "打包 ab-claude-dev.plugin（含 repos 上下文，需約 30 秒）",
					"script": "bash scripts/build-claude-dev-plugin.sh",
					"phases": ["Skills", "Agents", "Hooks", "Rules", "Repos", "打包完成"],
					"successMsg": "ab-claude-dev.plugin 打包完成 → dist/ab-claude-dev.plugin"
				}
			]
		},
		"zsh": {
			"label": "ZSH 環境模組",
			"hint": "conf → ~/.zshrc.d/ + sheldon 插件管理",
			"flag": "--zsh",
			"steps": [
				{
					"type": "install-modules",
					"label": "安裝 ZSH 模組 → ~/.zshrc.d/",
					"script": "zsh zsh/install.sh",
					"selectable": {
						"modules": {
							"dir": "zsh/.zshrc.d/conf",
							"ext": ".zsh",
							"selectLabel": "選擇要安裝的環境模組",
							"exclude": ["00-env", "90-plugins"]
						}
					},
					"extraTotal": {
						"base": 3,
						"ifModule": {
							"tools": 1
						}
					}
				}
			],
			"outroMsg": "✅ 安裝完成！執行 exec zsh 立即套用"
		}
	},
	"sources": [
		{
			"name": "everything-claude-code",
			"repo": "affaan-m/everything-claude-code",
			"paths": {
				"commands": "commands",
				"agents": "agents",
				"rules": "rules/{lang}",
				"rulesCommon": "rules/common",
				"hooks": "hooks/hooks.json"
			},
			"priority": 10
		}
	]
}
