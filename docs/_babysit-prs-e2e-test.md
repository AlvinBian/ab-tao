# babysit-prs 全鏈驗證用檔案

此檔案僅供 `/babysit-prs` 閉環機制 Wave 3 端到端驗證使用，驗證完成後即刪除，不併入正式文檔。

驗證項目：author 角色收到 review nit 後的自動修復鏈（Edit → lint/test → commit → push → 回覆 PR thread）。

故意留的小瑕疵：下面這句話的的用字重複，等待被指出並修正。
